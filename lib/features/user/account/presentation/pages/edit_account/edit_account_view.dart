import 'package:flutter/material.dart';

class EditUserAccountView extends StatelessWidget {
  const EditUserAccountView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('data'),
      ),
    );
  }
}
