import 'package:flutter/material.dart';
import 'widgets/dry_fruits_view_body.dart';

class DryFruitsView extends StatelessWidget {
  const DryFruitsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DryFruitsViewBody();
  }
}
