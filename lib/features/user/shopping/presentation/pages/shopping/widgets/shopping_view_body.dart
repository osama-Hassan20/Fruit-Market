import 'package:flutter/material.dart';

class ShoppingViewBody extends StatelessWidget {
  const ShoppingViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Center(
      child: Text('ShoppingView'),
    );
  }
}
